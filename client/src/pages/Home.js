const Home = () => {
    return(
        <div className = "home-page">
            {/* bootstrap classes can be used here. i have them add in the app component */}
            this is the home page
        </div>
    );
}

export default Home;